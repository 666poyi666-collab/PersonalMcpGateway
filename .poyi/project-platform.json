{
  "standardVersion": 1,
  "id": "personal-mcp-gateway",
  "name": "PersonalMcpGateway",
  "runtimeProject": true,
  "dataPolicy": "operational_metadata",
  "dataInventory": [
    {
      "id": "runtime-diagnostics",
      "classification": "internal",
      "dataPlane": "local_only",
      "mcpExposure": "tool",
      "coverage": "complete",
      "reason": "The five approved diagnostics tools read live Windows process, module, and fleet state. They are available only while the local Gateway is running, including through its authenticated tunnel."
    },
    {
      "id": "last-heartbeat",
      "classification": "internal",
      "dataPlane": "snapshot_mirror",
      "mcpExposure": "none",
      "coverage": "exempt",
      "reason": "A future cloud control plane may retain only the last sanitized observation with observedAt, expiry, stale, and offline semantics; it is never authoritative live state."
    },
    {
      "id": "audit",
      "classification": "internal",
      "dataPlane": "snapshot_mirror",
      "mcpExposure": "none",
      "coverage": "exempt",
      "reason": "A future cloud control plane may retain bounded sanitized audit records, but no diagnostics payload, runtime revision, secret, device identifier, or support bundle."
    },
    {
      "id": "dashboard-profile",
      "classification": "private",
      "dataPlane": "cloud_primary",
      "mcpExposure": "none",
      "coverage": "partial",
      "reason": "The browser implements an encrypted SyncEnvelopeV1 dashboard-profile entity with an atomic local outbox, cursor, conflicts, and bidirectional exchange boundary for layout, tile sizing, pinned projects and preferences. No cloud authority transport, pairing flow, or remote verification is configured; runtime diagnostics remain local-only."
    }
  ],
  "runtimeDiagnostics": {
    "dataPlane": "local_only",
    "allowedCloudRecords": [
      "last-heartbeat",
      "audit"
    ],
    "createsStateWhilePcOff": false
  },
  "mcp": {
    "status": "complete",
    "dataPlane": "local_only",
    "localBaseUrl": "http://127.0.0.1:8760",
    "localHealthBaseUrl": "http://127.0.0.1:8761",
    "cloudBaseUrl": null,
    "remoteTransport": "authenticated_tunnel",
    "routes": {
      "mcp": "/mcp",
      "health": "/healthz",
      "ready": "/readyz",
      "oauthResourceMetadata": "/.well-known/oauth-protected-resource/mcp"
    }
  },
  "sync": {
    "status": "partial",
    "dataPlane": "cloud_primary",
    "authority": "cloud",
    "routes": {
      "exchange": "/sync/v2/exchange",
      "status": "/sync/v2/status"
    },
    "contractId": "sync-envelope-v1",
    "envelopeVersion": 1,
    "mode": "encrypted_dashboard_profile_local_staged_remote_unverified",
    "localImplementationStatus": "browser_encrypted_outbox_exchange_boundary_implemented",
    "remoteVerificationStatus": "missing",
    "supportsPcOff": false,
    "supportsBidirectionalDelta": false
  },
  "knownGaps": [
    "Windows shutdown stops the Gateway and no new heartbeat, runtime revision, module health, or fleet state may be generated while it is off.",
    "The encrypted dashboard profile has a local exchange boundary but no configured cloud authority transport, pairing flow, or remote verification, so this project cannot claim PC-off dashboard-profile synchronization."
  ]
}
